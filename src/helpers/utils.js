export const convertHectoToKilo = (num) => {
  if (!num) return null;
  return (num / 10).toFixed(1);
};
export const convertDeciToMeter = (num) => {
  if (!num) return null;
  return (num / 10).toFixed(1);
};
